---
description: 遇到问题要修根因，禁止用旁路绕开；做不完可以留接口
alwaysApply: true
---

# 面对问题，不要绕开

修 bug、接能力、改架构时，先对准真正缺的那一层，再动手。不要为了让当前请求马上变绿，换一条不该承担这件事的路径。

- 先说清楚根因，再改代码。
- 根因在 Redis / 配置 / 协议 / 调用方，就修那一层；不要把职责塞进 Postgres、内存、客户端或别的模块顶上。
- 当前范围做不完：留下接口、TODO、空实现或明确的未接线入口，写清以后谁来填。不要用临时存储、静默降级、假成功、复制一套逻辑来“先跑起来”。
- 用户指出绕开时，撤回旁路，回到正确位置重做。

错误例子：refresh token 本应在 Redis，却因 Development 没配 Redis 就写进 Postgres，401 表面上没了，职责错了。

正确例子：Development 连 Redis；token 仍只进 Redis。Redis 还没装好就启动失败或留配置接口，而不是换库硬扛。
