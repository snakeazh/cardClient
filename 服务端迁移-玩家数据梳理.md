# 玩家数据服务端迁移梳理文档

> 目的：梳理当前**客户端本地**保存的玩家进度数据（解锁角色、关卡进度、金币、体力、圣物解锁、天赋及天赋等级），为后续迁移到服务端提供数据模型与改造依据。
>
> 梳理范围：`Card/Assets/App`（业务）+ `Card/Assets/Framework/Save`（存档框架）+ `Config/`（设计配置表）。

---

## 1. 现状：存档机制总览

### 1.1 存储方式

- **不是文件存档**，而是 Unity `PlayerPrefs` KV（Windows 上为注册表，真机为 plist/xml）。
- 复杂对象由调用方用 `JsonUtility` 序列化为 JSON 字符串，存进一个 key；简单数值直接 `GetInt/SetInt`。
- **没有统一存档文件、没有多存档位、没有用户 ID / 账号概念**。
- 存档框架：`Card/Assets/Framework/Save/`
  - `ISaveService`：平台无关 KV 接口（`HasKey / Get/SetString / Get/SetInt / Get/SetFloat / DeleteKey / Save`）。
  - `PlayerPrefsSaveService`：当前唯一实际使用的实现。
  - `WeChatSaveService`：**空壳**，所有方法抛 `NotImplementedException`；`SaveFramework.Create()` 按 `#if WECHAT_MINIGAME` 选择实现。
- 版本管理：无存档版本字段，靠 **key 名后缀 `.v1`** 演进；唯一的数据迁移逻辑在关卡进度（旧字段 `HighestClearedLevel` 作为新格式 `DifficultyProgress` 为空的兜底，见 `Level/LevelProgressService.cs:264-271`）。

### 1.2 读写时机

- **启动**：`AppBootstrap.cs` 中逐个 `new` 服务并 `Load()`（`:49-210`），注册进 `ServiceContainer`；`AppServicesHost`（常驻）`Track()` 所有可落盘服务（`AppServicesHost.cs:66-72`）。
- **写**：所有修改只改内存并置 `_dirty` 标记；`OnApplicationPause / OnApplicationFocus(false) / OnApplicationQuit / OnDestroy` 时统一 `FlushAll()`（`AppServicesHost.cs:74-102`）。少数关键点立即 `Save()`（如过关时 `GameSession.cs:6460`）。
- 架构特点：每个服务都是「**接口 + 内存态 + 脏标记**」结构。迁移时把 `Load()` 换成向服务端拉取、`Save()` 换成上报即可，业务调用点基本不用动。

### 1.3 全部存档 Key 清单（共 11 个业务 key）

| Key | 格式 | 所属服务 | 内容 |
|---|---|---|---|
| `wallet.gold.v1` | 裸 int | WalletService | 局外金币 |
| `energy.v1` | JSON | EnergyService | 体力 |
| `bag.v1` | JSON | BagService | 通用背包（**预留，目前无业务使用**） |
| `talent.v1` | JSON | TalentService | 天赋 |
| `level.progress.v1` | JSON | LevelProgressService | 关卡进度 + 角色解锁 + 最近使用 |
| `unlock.condition.v1` | JSON | UnlockConditionService | 圣物/收藏账号级解锁进度 |
| `score.v1` | JSON | ScoreService | 局内积分（跨重启保留） |
| `adshop.v1` | JSON | AdShopService | 每日广告领取计数 |
| `guide.progress.v1` | JSON | GuideProgressService | 新手引导进度 |
| `audio.bgm.enabled.v1` / `audio.sfx.enabled.v1` | 裸 int | AudioService | 音频设置 |
| `game.playback.speed.v1` | 裸 float | GameTableViewModel | 战斗播放速度设置 |

---

## 2. 玩家数据明细（迁移核心）

### 2.1 金币（Wallet）

- 位置：`Card/Assets/App/Wallet/WalletService.cs:11`，key `wallet.gold.v1`，**不用 JSON 直接存 int**（Load 时负数归零）。
- 接口（`IWalletService`）：`int Gold`、`event Action Changed`、`Add(int)`、`bool TrySpend(int)`（余额不足返回 false；无上限、无其他币种）。

**金币流动链路（服务端需接管的关键点）：**

| 流向 | 触发点 | 数值来源 |
|---|---|---|
| 产出 | 战斗结算 `BattleResultPopupViewModel.cs:86,132-151` | 局内积分 ÷ `GameConst.ExchangePointsForGoldCoins`（10 积分 = 1 金币），胜利打开弹窗即发，失败点返回也发 |
| 产出 | 广告商店 `AdShopService.cs:57-87` | `GameConst.AdShopGoldPerBuy` × 每日限 `AdShopGoldDailyLimit` 次（当前为模拟广告，未接 SDK） |
| 产出 | 新手引导兜底 `GuideService.cs:495-509` | 引导流程把钱包补足到当前抽卡价 |
| 消耗 | 天赋抽取 `TalentPopupViewModel.cs:163-191` | 价格递增：`TalentChestNeedGold + DrawCount × TalentNeedChestGold`（即 50 × 已抽次数） |
| 局内消耗 | 局内金币为**另一套**（`GameSession` 内存态，初始 = `PlayerInitialGoldNum` + 天赋/英雄词条），**不落盘**，不迁移 | — |

### 2.2 体力（Energy）

- 位置：`Card/Assets/App/Energy/`，key `energy.v1`，JSON。

`EnergySaveData`（`EnergyModels.cs:73-79`）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `Current` | int | 当前体力。Load 时仅防负数；**商店购买可超上限，不按 Max 截断** |
| `GameDay` | int | 游戏日编号 = 本地时间前移 `DailyResetHour` 小时后距 2020-01-01 的天数（`EnergyService.cs:162-166`） |
| `AdRefillCount` | int | 当日已看广告回满次数 |

**恢复机制（重点风险）**：
- **不是按时间戳逐点恢复**，而是「**跨日回满**」——读属性时懒检查 `EnsureDailyReset()`（`EnergyService.cs:146-159`），游戏日变了就回满 + 清零广告次数。未记录任何时间戳。
- 常量（`GameConst`）：`EnergyMax=5`（每日回满到此值）、`EnergyCostPerRun=1`（每局消耗）、`EnergyDailyResetHour=5`（凌晨 5 点跨日）、`EnergyAdRefillDailyLimit=3`。
- 广告商店另有 `AdShopStaminaPerBuy=10`（每次购买 10 点）/ `AdShopStaminaDailyLimit=10`（每日 10 次），与上面 `AdRefillCount` 是**两套独立计数**。
- ⚠️ **迁移风险**：当前完全依赖**客户端本地时钟**推算 GameDay，改库 / 调时间即可白嫖体力。服务端化必须改为服务器时间 + 时间戳。

### 2.3 关卡进度 + 角色解锁（LevelProgress）

- 位置：`Card/Assets/App/Level/`，key `level.progress.v1`，JSON。
- 关卡规模：难度 1–30 × 每难度 10 关 = 300 关（`LevelConfig`，Id 规则 = 难度 × 1000 + 关卡号）。**没有星级系统**。

`LevelProgressSaveData`（`LevelModels.cs:119-130`）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `ClearedDifficulties` | int[] | 已整难度通关的难度号集合（可由最高关推导，Save 时冗余写出） |
| `LastHeroId` | int | 最近使用的角色（0 = 无，进游戏按 `GameConst.DefaultHeroId` 兜底） |
| `LastLevelId` | int | 最近玩的关卡 Id |
| `LastDifficulty` | int | 最近难度（<=0 读入时按 1） |
| `UnlockedHeroIds` | int[] | **已解锁角色列表** |
| `HighestClearedLevel` | int | 旧版兼容字段：仅默认难度的最高通关关 |
| `DifficultyProgress` | `{int Difficulty; int HighestClearedLevel}[]` | 新格式：每个难度的最高通关关 |

**解锁规则（服务端需复刻的判定逻辑）**：
- 难度解锁：最低难度默认开；上一难度 `IsCleared`（打完全部 10 关）才开下一难度（`LevelProgressService.cs:48-70`）。
- 关卡解锁：难度已解锁 + 第 1 关默认开 + 须先通同难度上一关（`LevelProgressService.cs:152-170`）。
- **角色解锁**：`TryUnlockHero(heroId)` 只往 `UnlockedHeroIds` 里加（`LevelProgressService.cs:141-150`）。解锁条件是 `HeroConfig.UnlockCondition`（= 要求通关的难度号；`LevelUIViewModel.cs:341-349` 用 `IsCleared(hero.UnlockCondition)` 判定）。
- 写入口：开局选关 `LevelUIViewModel.cs:363-398`（写 LastXxx + 扣体力）、过关 `GameSession.cs:6436-6477`（MarkCleared + 立即 Save）。

### 2.4 圣物解锁（UnlockCondition）

> 概念澄清：**圣物没有"已拥有列表/等级"**。圣物是**局内获取**（`Run.RelicConfigIds`，对局结束即弃）。持久化的是「**账号级解锁进度**」——解锁后圣物才会出现在局内商店货池中。

- 位置：`Card/Assets/App/Unlock/`，key `unlock.condition.v1`，JSON。

`UnlockConditionSaveData`（`UnlockConditionService.cs:293-298`）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `Progress` | `{int ConditionId; int Amount}[]` | 每个解锁条件的累计进度 |
| `UnlockedRelicIds` | int[] | 已解锁圣物 Id 快照（加载后再按进度重算补齐） |

机制：
- 配置：`UnlockConditionConfig`（22 行）`{Id, Type(ContidionType 枚举 1–22：杀敌/搓牌/刷新商店/通关难度/累计金币/暴击次数…), StackedValue, Value, Desc}`。
- `Report(type, amount)`（`UnlockConditionService.cs:103-159`）：累加类进度 `+= amount × StackedValue`；取最大类（通关难度/持有金币/单次伤害）`进度 = max(当前, amount)`。
- 每局开局 `BeginRun()` 拍商店货池快照（`:77-89`）：**局内新解锁的不进本局货池**。
- 局内新解锁进 `_pendingUnlockRelicIds`，回主页弹窗展示（`HomeViewModel.cs:77-101`）；图鉴页也读它判断解锁显示。
- 上报点遍布 `GameSession`（20+ 处 `UnlockSvc().Report(...)`：搓牌 `:633`、刷新商店 `:2164/2176`、牌型 `:3061-3099`、金币累计 `:3236-3237`、击杀 `:5192/5698`、通关难度 `:6617` 等），全部只改内存靠脏标记落盘。

⚠️ **迁移风险**：所有解锁条件均由客户端自行上报累计，天然易被篡改，服务端化后必须改为服务端基于对局结算数据计算。

### 2.5 天赋及天赋等级（Talent）

- 位置：`Card/Assets/App/Talent/`，key `talent.v1`，JSON。
- 配置：31 种天赋 × 10 级（`TalentConfig` + 机制表 `TalentEntryConfig`）。

`TalentSaveData`（`TalentModels.cs:101-108`）：

| 字段 | 类型 | 含义 |
|---|---|---|
| `Entries` | `{int TalentId; int Count}[]` | 每个天赋已拥有份数 |
| `DrawCount` | int | 累计抽天赋次数，决定下次抽取价格 |

**等级推导规则（重点）**：
- **存档不直接存等级**。等级 = `min(MaxLevel, Count / CopiesPerLevel)`，当前 `CopiesPerLevel = 1`（`TalentService.cs:53-63`、`TalentBalance.cs:10`），即份数 = 等级。
- 超出满级的份数会保留但不升级（为后续 `CopiesPerLevel` 调整预留）。
- 价格递增模型：`抽取价 = TalentChestNeedGold(0) + DrawCount × TalentNeedChestGold(50)`（`TalentService.cs:124-133`）。
- 无重置接口（只有 `Clear()` 清档用）。
- 生效方式：`TalentBonusManager`（无持久化）把已拥有天赋的词条（`TalentEntryConfig.Value × 等级`）汇总成英雄面板加成（攻击/暴击/闪避/生命），局外选角和局内开局共用。

⚠️ **迁移风险**：「扣金币」和「发天赋 + DrawCount+1」在客户端是两个内存操作，服务端化必须做成**原子事务**并做价格校验（防重放/防改 DrawCount 压价）。

### 2.6 背包（Bag，预留）

- 位置：`Card/Assets/App/Bag/`，key `bag.v1`，JSON：`{Entries: [{int ItemId; int Count}]}`。
- ⚠️ 除 Bootstrap 注册外**全工程无任何业务使用**，是预留的通用背包。迁移排期可放低优先级，但模型可一并设计。

### 2.7 其他需一并迁移的状态

| Key | 内容 | 说明 |
|---|---|---|
| `score.v1` | 局内积分 | **跨重启保留**（`ScoreService`），结算时按 `ExchangePointsForGoldCoins` 换算金币；服务端化后建议改为服务端记账，客户端不再持币 |
| `adshop.v1` | 每日广告领取计数 | 与体力/金币的每日限制挂钩，**同样依赖本地时钟** |
| `guide.progress.v1` | 新手引导进度 | 弱相关，建议一并服务端化 |

---

## 3. 配套配置表（服务端同样需要）

配置链路：`Config/*.xlsx`（策划编辑）→ 一键导出 → `TempConfig/csv|json` + 运行时 `Card/Assets/Res/Config/*.json` + 生成类 `Card/Assets/App/Config/Generated/*.cs`。共 19 张表（清单见 `ConfigTables.cs`）。

与玩家进度直接相关的表：

| 表 | 行数 | 用途 |
|---|---|---|
| `GameConst` | 常量对象 | 体力上限/消耗/重置时刻、广告产出限次、积分→金币汇率、天赋价格、初始金币等全部全局数值 |
| `HeroConfig` / `HeroEntryConfig` | 12 | 角色属性、解锁条件（要求通关难度） |
| `LevelConfig` | 300 | 关卡（难度/怪物组/通关金币/机制条数） |
| `MonsterConfig` / `MonsterGroupConfig` / `MonsterLogicConfig` | ~2.5 万 | 怪物数值 / 怪物组 / AI 下注行为 |
| `BossEntryConfig` | 138 | 关卡机制（debuff 词缀） |
| `TalentConfig` / `TalentEntryConfig` | 310 / 31 | 天赋等级行（品质/宝箱概率/价格）/ 机制数值 |
| `RelicConfig` / `RelicEntryConfig` | 118 | 圣物（品质/商店概率/买卖价/解锁条件/机制） |
| `ItemConfig` | 3 | 技能道具（搓牌/透视/替换） |
| `UnlockConditionConfig` | 22 | 解锁/成就条件（类型/叠加值/达成值） |
| `CollectionConfig` | 9 | 图鉴-收藏条目 |
| `HandScoreConfig` | 6 | 牌型倍率 |
| `GuideGroupConfig` / `GuideStepConfig` | — | 新手引导 |

> 注意：`TempConfig/json/MechanismConfig.json` 是残留旧表，无源 xlsx、不在加载清单，迁移忽略。

---

## 4. 建议的服务端数据模型

建议按「**账号主档 + 对局结算上报**」设计，客户端登录拉取全量、局内不持关键币：

```jsonc
// PlayerProfile（账号主档，对应原 level.progress + wallet + energy + talent + unlock）
{
  "userId": "string",
  "saveVersion": 1,                    // 替代 key 后缀 .v1 的版本演进
  "gold": 0,                           // wallet.gold.v1
  "energy": {                          // energy.v1
    "current": 5,
    "lastResetTime": 1700000000,       // ⚠️ 新增：服务器时间戳，替代客户端 GameDay
    "adRefillCount": 0
  },
  "adShop": { "goldClaimed": 0, "staminaClaimed": 0, "lastResetDate": "2024-01-01" },
  "levelProgress": {                   // level.progress.v1
    "difficultyProgress": [ { "difficulty": 1, "highestClearedLevel": 10 } ],
    "clearedDifficulties": [1],
    "unlockedHeroIds": [1],
    "lastHeroId": 1, "lastLevelId": 1001, "lastDifficulty": 1
  },
  "talent": {                          // talent.v1
    "entries": [ { "talentId": 101, "count": 3 } ],  // 等级 = min(MaxLevel, count / CopiesPerLevel)
    "drawCount": 5
  },
  "unlockCondition": {                 // unlock.condition.v1
    "progress": [ { "conditionId": 10101, "amount": 20 } ],
    "unlockedRelicIds": [101]
  },
  "guideProgress": { },
  "bag": { "entries": [] }             // 预留
}
```

**关键接口建议**：

| 接口 | 说明 |
|---|---|
| `Login/GetProfile` | 登录拉取全量主档（含体力按服务器时间推算后的当前值） |
| `Battle/Settlement` | 对局结算上报（局内积分、击杀数、牌型次数、金币累计、通关难度等原始数据），**由服务端计算**金币产出、圣物解锁条件进度、关卡通关——替代客户端 `Report()` 自报 |
| `Talent/Draw` | 服务端校验价格（基于 DrawCount）→ 原子扣金币 + 发天赋 + DrawCount+1 |
| `Energy/RefillByAd`、`AdShop/Claim` | 服务端校验每日限制（服务器日期） |
| `Hero/Unlock` | 服务端校验解锁条件（通关难度）后写入 |

---

## 5. 迁移方案与风险点

### 5.1 主要风险点

1. **本地时钟依赖（最高优先级）**：体力跨日回满、广告每日限次、AdShop 计数全部基于客户端时钟（`GameDay` 推算）。服务端化必须改为**服务器时间戳 + 上次重置时间**，每日重置逻辑收归服务端。
2. **客户端自报进度（防作弊）**：圣物解锁条件的 20+ 处 `Report()`、积分→金币换算，目前都在客户端完成。服务端化后应改为**对局结算时上报原始对局数据，由服务端重算**，客户端只负责展示。
3. **操作原子性**：天赋抽取「扣金币 + 发天赋 + DrawCount+1」、体力「扣体力 + 开局」必须做成服务端事务，并保证接口**幂等**（防网络重试导致重复扣/发）。
4. **价格递增模型的校验**：`DrawCount` 决定价格，若客户端可篡改 DrawCount 即可压价，必须由服务端持有并校验。
5. **弱网/断线**：当前是本地内存 + 脏标记落盘，弱网无感；改服务端后需设计离线兜底（如暂存本地、连网补报，或要求强联网）。
6. **微信小游戏存档为空壳**：`WeChatSaveService` 未实现，若首发即服务端化可跳过该实现；若先上本地版再迁，微信端需另行处理。

### 5.2 客户端改造点（利用现有架构）

现有「接口 + 内存态 + 脏标记」结构对迁移友好，建议保留接口层、替换数据源：

- `Framework/Save/ISaveService` 保持不变，作为本地缓存层；新增网络数据源。
- 各服务的 `Load()`：改为 `Login/GetProfile` 拉取后填充内存态。
- 各服务的 `Save()`（`ISaveFlushable`）：改为变更上报；`AppServicesHost.FlushAll()` 触发点可复用为「批量同步」时机。
- 业务调用点（选关、结算弹窗、天赋抽取等）**不需要改动 UI 层**，只需服务内部实现换源。
- 结算链路改造最大：`GameSession` 中 20+ 处 `UnlockSvc().Report()` 与积分结算需收敛为一次 `Battle/Settlement` 上报。

### 5.3 建议迁移步骤

1. **定义协议与数据模型**（本文档第 4 节为起点），确定强联网 or 弱联网兜底策略。
2. **配置表同步**：建立服务端配置表加载（xlsx 导出链路已有，可直接复用 `TempConfig/json` 产物）。
3. **服务端实现核心接口**：登录拉档、结算上报（金币/关卡/解锁条件一体）、天赋抽取、体力/广告每日逻辑。
4. **客户端双写灰度**：本地 PlayerPrefs 与服务端并行写，对比数据一致性；老玩家存档一次性导入（读本地 `*.v1` → 上报服务端）。
5. **切读**：验证完成后客户端只读服务端数据，本地存档降级为缓存/离线兜底。
6. **清理**：下线本地存档路径，移除编辑器外挂入口（`GameCheatMenu.cs` 的金币/天赋作弊项在联网版必须屏蔽）。

---

## 附：关键文件索引

| 模块 | 文件 |
|---|---|
| 存档框架 | `Card/Assets/Framework/Save/SaveFramework.cs`、`AppServicesHost.cs`（在 `Card/Assets/App/Bootstrap/`） |
| 启动注册 | `Card/Assets/App/Bootstrap/AppBootstrap.cs:49-210` |
| 金币 | `Card/Assets/App/Wallet/WalletService.cs` |
| 体力 | `Card/Assets/App/Energy/EnergyService.cs`、`EnergyModels.cs` |
| 关卡/角色 | `Card/Assets/App/Level/LevelProgressService.cs`、`LevelModels.cs` |
| 圣物解锁 | `Card/Assets/App/Unlock/UnlockConditionService.cs` |
| 天赋 | `Card/Assets/App/Talent/TalentService.cs`、`TalentModels.cs`、`TalentBalance.cs` |
| 广告商店 | `Card/Assets/App/AdShop/AdShopService.cs` |
| 结算发金币 | `Card/Assets/App/UI/Popup/BattleResultPopupViewModel.cs:86,132-151` |
| 天赋抽取 | `Card/Assets/App/UI/Popup/TalentPopupViewModel.cs:163-191` |
| 全局常量 | `Config/GameConst.xlsx` → `Card/Assets/App/Config/Generated/GameConst.cs` |
| 编辑器外挂 | `Card/Assets/App/Game/Editor/GameCheatMenu.cs` |
